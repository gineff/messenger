<div class="chat__header chat-header {{empty}}">
  <Chat.Avatar className="chat-header__avatar" image="{{chat.avatar}}" />
  <div class="chat-header__title">{{chat.title}}</div>
  <Button className="chat-header__menu" />
</div>
