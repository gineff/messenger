{
  "name": "messenger",
  "version": "0.0.1",
  "description": "flib.chat",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "parcel ./static/index.html --no-cache",
    "build": "parcel build ./static/index.html --no-optimize",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gineff",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "express": "^4.18.1",
    "path": "^0.12.7"
  }
}
